#!/usr/bin/env bash

set -e

GIT_REMOTE=git@github.com:n0bra1n3r/dotfiles.git

cd "{{ .chezmoi.sourceDir }}"

git remote set-url origin $GIT_REMOTE && \
  echo "> git remote set-url origin $GIT_REMOTE"

# vim: filetype=bash
